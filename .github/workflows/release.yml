name: Release

on: 
  workflow_dispatch:
    inputs:
      release_type:
        description: 'Release Type'
        required: true
        default: 'stable'
        type: choice
        options:
          - stable
          - pre-release
      dry_run:
        description: 'Dry Run? (Validate only, no upload)'
        required: false
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  run-release:
    name: Run release
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [20]
    steps:
      - name: Check out Git repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Get Version
        id: version
        run: echo "prop=$(node -p "require('./package.json').version")" >> $GITHUB_OUTPUT

      - name: Package Extension
        run: pnpm dlx @vscode/vsce package --no-dependencies

      - name: Get VSIX Path
        id: vsix_file
        run: echo "name=$(ls *.vsix)" >> $GITHUB_OUTPUT

      - name: Upload Extension Artifact
        uses: actions/upload-artifact@v4
        with:
          name: svn-blamer-${{ steps.version.outputs.prop }}
          path: "*.vsix"
          retention-days: 5

      - name: Publish to Open VSX Registry
        if: ${{ inputs.release_type == 'stable' }}
        uses: HaaLeo/publish-vscode-extension@v1
        with:
          pat: ${{ secrets.OPEN_VSX_TOKEN }}
          dryRun: ${{ inputs.dry_run }}
          packagePath: ${{ steps.vsix_file.outputs.name }}

      - name: Publish to Visual Studio Marketplace
        uses: HaaLeo/publish-vscode-extension@v1
        with:
          pat: ${{ secrets.VS_MARKETPLACE_TOKEN }}
          preRelease: ${{ inputs.release_type == 'pre-release' }}
          registryUrl: https://marketplace.visualstudio.com
          dryRun: ${{ inputs.dry_run }}
          packagePath: ${{ steps.vsix_file.outputs.name }}